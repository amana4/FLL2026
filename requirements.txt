# Version ranges, not exact pins: these allow bugfixes but block the next
# major version from silently breaking the build.
mkdocs-material>=9.5,<10
mkdocs-same-dir>=0.1.3,<0.2

# MkDocs 2.0 is a rewrite that drops the plugin system entirely, with no
# migration path — it would break same-dir and the Material theme. mkdocs is an
# indirect dependency here, so pin it explicitly rather than relying on
# mkdocs-material's own range. Revisit only if the ecosystem settles.
mkdocs>=1.6,<2
