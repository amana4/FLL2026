Exported SPIKE Prime `.llsp3` project files go here.

Name each after its run: `R1-north-field.llsp3`, `R2-cargo-line.llsp3`, etc.

Git can't show what changed inside these files — always commit a matching
screenshot in `../screenshots/` so teammates can review the change.

See `../README.md` for the full workflow.
